#include "main.h"

/**
 * _islower - A function that checks for lowercase characters
 * @c: The character to check
 * Return: 1 if c is lowercase and 0 if it id not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
